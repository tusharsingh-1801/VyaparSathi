# Server
PORT=4000

# Supabase (server-side only — NEVER expose these to the Expo frontend)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Sarvam AI (server-side only — NEVER expose this to the Expo frontend)
SARVAM_API_KEY=

# Optional overrides (safe defaults are used if left blank)
# SARVAM_API_URL=https://api.sarvam.ai/v1/chat/completions
# SARVAM_MODEL=sarvam-105b
